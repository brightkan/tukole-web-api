from inow.settings import *

DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '{{ name }}',
        'USER': '{{ name }}',
        'PASSWORD': '{{ secret }}',
        'HOST': '127.0.0.1',
        'PORT': '5432',
    }
}

